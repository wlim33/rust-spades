/target
**/*.rs.bk

# Deploy secrets — never check this into the repo.
/.deploy.env
*.deploy.env

# Local dev database (created by `make dev`).
/dev.sqlite*

# Cloudflare Wrangler local cache (not for version control).
/.wrangler/
